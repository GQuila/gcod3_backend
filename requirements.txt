# GCOD3 backend dependencies (production)
# All packages installable from public PyPI - works on Render, Fly, Railway, VPS, Docker.

# ─── Core framework ──────────────────────────────────────────
fastapi==0.110.1
uvicorn==0.25.0
python-dotenv>=1.0.1
python-multipart>=0.0.9
httpx>=0.27.0

# ─── Data layer ──────────────────────────────────────────────
motor==3.3.1
pymongo==4.6.3
pydantic>=2.6.4
email-validator>=2.2.0

# ─── Auth & security ─────────────────────────────────────────
bcrypt==4.1.3
pyjwt>=2.10.1
cryptography>=42.0.8
google-auth>=2.30.0

# ─── AI provider (official OpenAI SDK — public PyPI, works on Render) ───
# We call the Emergent universal-key proxy which is OpenAI-compatible and
# routes to Claude / OpenAI / Gemini under the hood. Override the base_url
# via the AI_BASE_URL env var to talk to any other OpenAI-compatible host.
openai>=1.50.0

# ─── Misc utils ──────────────────────────────────────────────
tzdata>=2024.2
