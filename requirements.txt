# GCOD3 backend dependencies (production)
# All packages installable from public PyPI - works on Render, Fly, Railway, VPS, Docker.

# ─── Core framework ──────────────────────────────────────────
fastapi==0.110.1
uvicorn==0.25.0
python-dotenv>=1.0.1
python-multipart>=0.0.9
httpx>=0.27.0

# ─── Data layer ──────────────────────────────────────────────
motor==3.3.1
pymongo==4.6.3
pydantic>=2.6.4
email-validator>=2.2.0

# ─── Auth & security ─────────────────────────────────────────
bcrypt==4.1.3
pyjwt>=2.10.1
cryptography>=42.0.8

# ─── AI provider (Emergent universal key — Anthropic/OpenAI/Gemini via one key) ──
# Installed from Emergent's public package index (see render.yaml buildCommand).
emergentintegrations

# ─── Misc utils ──────────────────────────────────────────────
tzdata>=2024.2
